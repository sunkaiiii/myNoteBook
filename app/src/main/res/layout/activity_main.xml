<?xml version="1.0" encoding="utf-8"?>
<android.support.design.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context="com.example.sunkai.mynotebook.MainActivity">
    <ListView
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:id="@+id/notelistview" />

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end">
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="mini"
        android:layout_gravity="bottom|end"
        android:layout_margin="36sp"
        app:srcCompat="@drawable/ic_add_white_24dp" />
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="mini"
        android:layout_alignBottom="@+id/fab"
        android:visibility="gone"
        android:layout_toRightOf="@id/fab"
        android:layout_marginLeft="-21sp"
        app:backgroundTint="@color/transparent_red"
        app:srcCompat="@drawable/ic_text_fields_white_24dp"  />
        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:fabSize="mini"
            android:visibility="gone"
            android:layout_toRightOf="@id/fab_text"
            android:layout_alignBottom="@+id/fab"
            android:layout_marginLeft="10sp"
            app:backgroundTint="@color/transparent_red"
            app:srcCompat="@drawable/ic_crop_original_white_24dp" />
        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab_vedio"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:fabSize="mini"
            android:visibility="gone"
            android:layout_toRightOf="@id/fab_image"
            android:layout_alignBottom="@+id/fab"
            app:backgroundTint="@color/transparent_red"
            android:layout_marginLeft="10sp"
            app:srcCompat="@drawable/ic_camera_alt_white_24dp" />
    </RelativeLayout>

</android.support.design.widget.CoordinatorLayout>
